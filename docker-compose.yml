# Multi-Agent Chatbot stack.
#
# Services:
#   redis   - broker + result backend for Celery
#   api     - FastAPI HTTP layer (dispatches tasks, polls results)
#   worker  - Celery worker that runs the LangGraph agent graph
#   ui      - Streamlit frontend
#
# Ollama is NOT containerized: it runs natively on the host so it can use the
# GPU. Containers reach it via `host.docker.internal:11434`.
#
# Usage:
#   docker compose up --build           # first run or after dep/code changes
#   docker compose up -d                # detached
#   docker compose logs -f worker       # tail a specific service
#   docker compose up -d --scale worker=4
#   docker compose down                 # stop, keep Redis data
#   docker compose down -v              # stop and wipe Redis data
#
# RAG source uploads (chroma-data volume, worker only): Chroma's
# PersistentClient is a single-writer, SQLite-backed embedded store, so
# `--scale worker=4` above is NOT safe to use once source uploads are in use
# -- multiple worker processes must not open the same Chroma persist dir
# concurrently. Keep worker at 1 replica, or swap to a Chroma server
# container + HttpClient before scaling workers horizontally.

services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      # Host-side port shifted to 6380 to avoid colliding with a host-level
      # redis-server that may already own 6379. Only affects host->container
      # access (e.g. redis-cli from the host); api/worker talk to this
      # container over the internal Docker network at redis:6379, unaffected.
      - "6380:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  api:
    build: .
    image: multiagent-chatbot:latest
    command: uvicorn src.api:app --host 0.0.0.0 --port 5000
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      REDIS_URL: redis://redis:6379
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      API_HOST: 0.0.0.0
      API_PORT: "5000"
    env_file:
      - .env
    extra_hosts:
      # Required on Linux hosts so host.docker.internal resolves. No-op on
      # Windows/macOS Docker Desktop where the alias exists by default.
      - "host.docker.internal:host-gateway"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:5000/health"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 10s

  worker:
    build: .
    image: multiagent-chatbot:latest
    command: celery -A src.tasks worker --loglevel=INFO --pool=solo -n multiagent@%h
    restart: unless-stopped
    volumes:
      - chroma-data:/data/chroma
    environment:
      REDIS_URL: redis://redis:6379
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      CHROMA_PERSIST_DIR: /data/chroma
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      redis:
        condition: service_healthy

  ui:
    build: .
    image: multiagent-chatbot:latest
    command: streamlit run app.py --server.address=0.0.0.0 --server.port=8501 --server.headless=true
    restart: unless-stopped
    ports:
      - "8501:8501"
    environment:
      API_BASE_URL: http://api:5000
    depends_on:
      api:
        condition: service_healthy

volumes:
  redis-data:
  chroma-data:
